import 'package:flutter/material.dart';

class SettingUser extends StatelessWidget {
  const SettingUser({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(child: Text("Ajustes de Usuario"));
  }
}
